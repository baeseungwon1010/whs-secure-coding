# CLAUDE.md — Tiny Second-hand Shopping Platform

이 파일은 새 세션(터미널 remote control 포함)이 컨텍스트를 재질문 없이 이어받기 위한
프로젝트 지침이다. **작업 시작 전 반드시 이 파일을 읽고 아래 결정 사항을 따를 것.**

---

## 1. 프로젝트 개요
- **과제**: 시큐어코딩 — Tiny Second-hand Shopping Platform(중고거래 플랫폼)
- **목표**:
  1. 아래 최소 요구사항(기능 8종 + 비기능 3종)을 모두 충족
  2. **보안 약점을 최대한 제거** (핵심 평가 요소)
  3. 개발 전 과정 **보고서** 작성(요구사항 분석·설계·구현·체크리스트/테스트·유지보수, 양식 자유)
  4. 발견한 보안 약점과 개선 방법을 보고서에 정리
  5. 완성본을 **public GitHub**에 올리고 README.md에 환경설정·실행방법 명시
- **AI 도구 적극 활용 권장.**

## 2. 확정된 기술/설계 결정 (사용자 승인 완료 — 임의 변경 금지)
| 항목 | 결정 |
|------|------|
| 기술 스택 | **Flask + SQLite + Flask-SocketIO**(실시간 채팅) |
| 진행 방식 | 설계·계획 먼저 → **단계적 구현** |
| 이메일(아이디/비번 찾기) | **SMTP 실제 전송** — 자격증명은 `.env`(예: Gmail 앱 비밀번호) |
| 송금 기능 | **가상 잔액 기반 모의 이체** — 외부 계좌/결제 API 없음 |
| 관리자 자동 처리(신고/비속어/비정상 거래 탐지) | **규칙·패턴 기반** — 외부 AI 없음 |
| 디자인 | 둥글둥글, **하늘색 계열**, CSS 기반 |

## 3. 요구사항 요약
### 기능 요구사항 (8)
1. 로그인/회원관리 — 회원가입(아이디·비번·닉네임·복구 이메일), 아이디 중복검사,
   아이디/비번 찾기(이메일), 프로필(이미지·소개글·비번 변경), 세션 인증
2. 상품 관리 — 등록/수정/삭제/카테고리, 마이페이지 내 상품 목록, 수정시간 기록,
   삭제 시 경고창+3초 후 버튼 활성화, 금지 키워드 필터→관리자 검토
3. 상품 검색·정렬 — 기본 최신순, 사진-상품명-가격 레이아웃, 상품명/키워드/정보/거리 검색,
   시간·가격·조회수 정렬, 핫게시물(기간 내 조회수 top), 최근 본 상품/검색어(5개 제한),
   상세페이지(정보+채팅+댓글/대댓글+찜+구매), 찜목록
4. 거래자 간 채팅 — 1:1 인스턴스 채팅, 비속어 필터(삐 처리·경고·제재), 신고/차단,
   알림 설정, 유휴 시 인스턴스 종료
5. 송금 — 가상 잔액 이체, 거래 확인 후 DB 변동 및 판매자 알림
6. 알림 — 판매/채팅/찜 알림, 수신 시간·내용 조절 동의
7. 신고 — 통합 신고 페이지(카테고리), 상품/채팅 개별 신고(제목·카테고리·상세·사진),
   패턴 기반 자동 처리→제재·알림
8. 관리자 모드 — 전체 상품 확인/삭제(사유 작성→유저 전달), 신고 채팅 제한적 확인,
   비정상 거래 탐지, 신고목록/답변/상태변경/알림, 회원관리·대시보드, 공지 작성·알림

### 비기능 요구사항 (3)
1. **보안**  2. 유저 친화적 둥글둥글 디자인(하늘색)  3. 가용성

### 부가 항목
- 로그 관리(로그인/회원가입 쿼리 로그, 상품등록/채팅 로그 DB 저장)
- 마이페이지(구매/판매 내역, 등록/대기 상품, 찜 목록, 회원정보 수정)
- 이미지 관리(시그니처 검사, 용량 제한, 미참조 이미지 삭제, jpg/jpeg/png만 허용, 경로 정규화)

## 4. 보안 원칙 (반드시 준수 — 핵심 평가 요소)
- **비밀번호**: 해시 저장(`werkzeug.security` 또는 bcrypt), 평문 저장·로그 금지
- **인증/세션**: 로그인 세션 관리, 구매·채팅 등 민감 동작에 `@login_required`, `Secure`/`HttpOnly`/`SameSite` 쿠키
- **SQL Injection**: 항상 **파라미터 바인딩**(문자열 포매팅 쿼리 금지)
- **XSS**: Jinja2 자동 이스케이프 유지, `|safe` 신중 사용, 사용자 입력 검증
- **CSRF**: Flask-WTF CSRF 토큰 등 모든 상태 변경 폼에 적용
- **파일 업로드**: 매직바이트(시그니처) 검사 + 확장자 화이트리스트(jpg/jpeg/png) + 용량 제한 + `secure_filename`/경로 정규화로 **path traversal 방지** + 실행권한 없는 저장경로
- **접근제어**: 관리자/본인 리소스 권한 검사(IDOR 방지)
- **비속어/금지 키워드 필터**: 채팅·상품 등록에 적용
- **입력 검증**: 서버 측 검증 필수(클라이언트 검증 신뢰 금지)
- **로그**: 인증/주요 이벤트 로그 남기되 민감정보 마스킹
- **비밀정보**: `.env`로 분리, git 커밋 금지(`.gitignore`), 저장소에 커밋할 `.env.example` 제공

## 5. 계획된 디렉터리 구조(방향, 구현 시 구체화)
```
secure-coding/
├─ app.py                # 엔트리포인트 / 앱 팩토리
├─ config.py             # 설정(.env 로드)
├─ requirements.txt
├─ .env.example          # 실제 .env는 커밋 금지
├─ schema.sql            # SQLite 스키마
├─ instance/             # SQLite DB 파일(gitignore)
├─ app/
│  ├─ __init__.py        # create_app, extensions 초기화
│  ├─ models/            # DB 접근 계층
│  ├─ blueprints/        # auth, products, search, chat, payment,
│  │                     # notifications, reports, admin, mypage
│  ├─ security/          # 파일검증, 비속어필터, 데코레이터
│  ├─ utils/             # email(SMTP), 공통 헬퍼
│  ├─ static/            # CSS(하늘색·둥글), JS, uploads/
│  └─ templates/         # Jinja2
└─ tests/                # 보안·기능 테스트 및 체크리스트
```

## 6. 실행 방법 뼈대 (README에서 확정)
```bash
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env          # SECRET_KEY, SMTP 자격증명 등 입력
python app.py                 # 개발 서버 (Flask-SocketIO)
```
`.env` 예상 키: `SECRET_KEY`, `DATABASE`, `SMTP_HOST/PORT/USER/PASSWORD`, `MAIL_FROM`,
`MAX_UPLOAD_MB`, `ADMIN_*`(초기 관리자 시드).

## 7. 다음 단계 (구현 순서 제안)
1. 프로젝트 스캐폴드(app factory, config, .env.example, requirements, schema.sql, base 템플릿/CSS)
2. 인증/회원관리(회원가입·로그인·세션·비번 해시·중복검사) + 아이디/비번 찾기(SMTP)
3. 상품 CRUD + 이미지 업로드 보안 + 마이페이지
4. 검색·정렬·상세(댓글/대댓글·찜·조회수·최근 본/검색어·핫게시물)
5. 채팅(SocketIO)·비속어 필터·차단
6. 송금(가상 잔액)·알림
7. 신고·관리자 모드·규칙 기반 자동 처리·대시보드·로그
8. 보안 점검 체크리스트/테스트 + 보고서 + README + public GitHub 배포

## 8. 협업 규칙
- 서버 측 입력 검증·파라미터 바인딩·권한 검사를 **모든 신규 엔드포인트에 기본 적용**
- 비밀정보/DB/업로드 파일은 커밋 금지(`.gitignore` 관리)
- 커밋/푸시는 사용자가 요청할 때만 수행
